# 全局规则

所有项目通用，必须遵循的底线规范。

---

## 语言规范

**所有交互全程使用简体中文**，包括但不限于：
- 思考过程（think）必须使用中文
- 交互选项、按钮标签、菜单等使用中文
- 对话回复、说明、解释使用中文
- 代码注释使用中文（当需要时）

---

## 编程准则

**禁止写空壳函数、模拟函数、硬编码。** 空壳函数和模拟函数必须包含完整实现，不能只是 `pass`、`return nil`、`返回零值` 或仅有注释而无实际逻辑。硬编码指在代码中直接使用未抽取的字符串字面量作为业务相关常量，应定义为常量或从配置/数据库动态读取。

**动态数据优先。** 业务相关配置（如字典表、枚举值等）应优先从数据库或配置中心动态读取，避免硬编码常量导致数据不一致。

---

## 任务处理

**新项目、新需求、新功能必须进入计划任务模式。** 收到此类请求时，立即使用 `EnterPlanMode` 进入规划阶段，将任务分解为清晰的步骤列表，列出每个步骤的具体内容和目标，与用户确认计划后再执行。禁止直接跳到实现阶段。

---

## 命令执行

**多命令连接**：使用分号 `;` 代替 `&&` 连接多条命令，分号前后加空格。例如：
- `go build -o server.exe ./cmd/server ; .\server.exe`
- `cd "path/to/project" ; git status`

---

## 模型使用

- **长任务、长流程**：使用 `minimax-m2.7` 模型
- **用户提及快速，高速，快速模式，高速模型等任意组合**（如"快点"、"速度优先"、"快速完成"、"高速模式"等）：使用 `minimax-m2.7-highspeed` 模型

---

## 问题解决授权

**自主权限放宽**：当遇到多次尝试（≥3次）始终无法解决的难题时，允许：
1. 自主使用 `WebSearch` 联网搜索资料，寻求解决方案
2. 自主批准原本需要用户确认的请求（如权限提升、安装依赖、执行命令等）
3. 事后向用户汇报解决方案和所做的授权操作

**为什么**：减少因权限等待导致的任务卡顿，提升问题解决效率。

---

## 通用规范索引

通用规范拆分到独立文件，按需引用：

| 规范 | 文件路径 |
|------|----------|
| 测试策略（务实原则） | `~/.claude/rules/testing.md` |
| AI 团队协作规范 | `~/.claude/rules/agent-teams.md` |
| 多项目上下文切换 | `~/.claude/rules/context-switch.md` |

### Skill

| Skill | 功能 | 触发方式 |
|-------|------|---------|
| `template-sync` | 规则模板同步 | 被动检测关键词 / 每天首次检查 / 手动 `sync` |

---

## 规则优先级

| 规则类型 | 说明 | 优先级 |
|---------|------|--------|
| 全局规则 | 所有项目通用的底线规范 | 更高（法律） |
| 项目规则 | 当前项目的具体决策 | 更低（章程） |

**冲突时**：全局规则优先，项目规则不得违背全局规则底线。

---

## 项目规则模板

### 新项目初始化步骤

1. 创建项目目录
2. 复制 `~/.claude/templates/project-rules-template.md` 到项目根目录为 `CLAUDE.md`
3. 根据项目类型修改项目特定部分（技术栈、架构、领地划分等）

### 模板文件位置

`~/.claude/templates/project-rules-template.md`

### 模板内容说明

模板包含以下部分：
- **项目特定配置**：项目名称、技术栈、架构类型
- **领地划分**：BE/FE/Doc/UI Designer 各自的工作目录
- **项目特定规则**：业务规则、禁止行为等
- **架构决策**：为什么选择当前架构

详见模板文件本身。

### 模板强制程度

| 层级 | 标记 | 说明 |
|------|------|------|
| 通用规范 | 无标记 | 强烈建议遵循 |
| 最佳实践 | ✅ | 强烈建议 |
| 视情况 | ⚠️ | 参考，可根据项目调整 |
| 禁止行为 | ❌ | 强制禁止 |

### 模板更新触发机制

当你在项目中做出以下行为时，思考是否需要同步更新模板：

**触发条件（满足任一即触发）**：
1. 发现了新的通用最佳实践
2. 规避了某个常见的坑
3. 引入了新的工具或规范
4. 花费超过 2 小时解决一个问题
5. 测试中发现的新坑

**判断标准**：
- 这个规则/坑能跨项目复用吗？
- 这个规则属于"通用底线"还是"项目特定"？
- 其他项目可能重蹈这个坑吗？

**3 个问题有 2 个"是" → 同步更新模板**

详见 `~/.claude/templates/project-rules-template.md` 中的 CHANGELOG 部分。
